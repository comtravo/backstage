name: backstage_build

on:
  push:
    paths:
      - Dockerfile
      - .github/workflows/backstage.yaml
    branches:
      - 'master'
      - 'ARCH-322-deploy-backstage'
  pull_request:
    paths:
      - Dockerfile
      - .github/workflows/backstage.yaml
    branches:
      - 'master'
      - 'ARCH-322-deploy-backstage'

  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: build
        run: yarn build
